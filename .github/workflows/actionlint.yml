name: Actionlint

on:
  pull_request:

jobs:
  actionlint:
    name: Run actionlint
    runs-on: ubuntu-latest
    steps:
      - uses: it-at-m/lhm_actions/action-templates/actions/action-actionlint@b2b49a8b4344e3b5ac90dd1ee9a0c59860c41383 # v1.0.16