name: Actionlint

on:
  pull_request:

jobs:
  actionlint:
    name: Run actionlint
    runs-on: ubuntu-latest
    steps:
      - uses: it-at-m/lhm_actions/action-templates/actions/action-actionlint@e76e0bde64af98eb44ccd5b6e048ce5d82f4acc4 # v1.0.13