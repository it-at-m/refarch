name: Actionlint

on:
  pull_request:

jobs:
  actionlint:
    name: Run actionlint
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - uses: it-at-m/lhm_actions/action-templates/actions/action-actionlint@7cb754b2056cebda6c36db344434f85be398c493 # v1.0.25
