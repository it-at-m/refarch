refarch:
  cosys:
    url: https://cosysk007.srv.muenchen.de/cosys-eai/rest
    merge:
      datafile: //multi
      inputLanguage: Deutsch
      outputLanguage: Deutsch
      keepFields: unresolved-ref

server:
  shutdown: graceful
  port: ${COSYS_INTEGRATION_SERVER_PORT:8093}